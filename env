#export RESTIC_REPOSITORY="sftp:<username>@<local_server>:/restic/$( { read h; printf '%s' "${h^^}"; } <<< "$(hostname)" )"
#export RESTIC_REPOSITORY="sftp:<username>@<tailscale_server>:/restic/$( { read h; printf '%s' "${h^^}"; } <<< "$(hostname)" )"
export RESTIC_PASSWORD_FILE="<password file>"
